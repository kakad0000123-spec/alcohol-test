# 2026-07-10 進度儀表板擴充

## 做了什麼
- 接續前一 session 未 commit 的 `dashboard/stats` WIP（區域×孔數/米數）繼續開發。
- 新增維度：扁鐵合計（長度 m + 重量 kg，`flatbarWeightG` 換算）、待寄/已寄、各廠商（admin）、近 8 週趨勢長條、admin 整體進度 bar。
- Nav 補「儀表板」連結一起 commit。部署 + 線上驗證（route 307、資料層彙總對 live DB 核對正確）。

## 為什麼
- 資料模型已演進出 `flatbar_mm` 但儀表板沒呈現，是明顯缺口。
- 進度儀表板該有「趨勢 + 廠商 + 材料 + 狀態」四面，原本只有區域一面。

## 踩了坑
- 無 admin 帳密可做 UI 登入截圖 → 改用本機 node 對 live DB 跑同一套彙總邏輯驗數字（23 筆/18.87m 對得上）。
- 前一 session 遺留未提交：`.claude/launch.json` 有 `xuanji`（別專案 dev server）條目 → 與本專案無關，未納入 commit。

## 否決了什麼/為什麼
- 否決「硬編死 TARGET_HOLES 當完成率分母」：全案目標數 Po 未確認，寫死會誤導。→ 暫定 1200 並在 UI 明標「暫定可調」，待 Po 拍板後再決定是否改成設定頁欄位。

## 追記（同日，Po 回饋後調整）
- 儀表板**拿掉「整體進度」與「待寄/已寄」兩欄**（Po 要先討論確認再上；整體進度分母未定、狀態統計待議）。儀表板維持詳細檢視（本週/累積/扁鐵卡片 + 近8週趨勢 + 各廠商 + 各區域）。
- **總覽**新增「本週完成 / 累積完成」方格（孔數+米）；獨立於顯示篩選另查、已套 scopeContractor（vendor 只算自己）。驗證：本週 17 孔/14.09m、累積 23 孔/18.87m。
- 總覽原有的「待寄/已寄」統計方格**保留未動**（Po 只指名儀表板那兩欄；「先拿掉…再上線」指的是尚未穩定上線的儀表板新元素）。

## 追記 2（廠商表單扁鐵欄位標單位）
- 扁鐵補修長度欄原本沒標單位 → 標題/placeholder/說明三處都加「單位 mm」（系統存的就是 flatbar_mm）。placeholder 改成較真實的 `300*2+150`。
- 踩坑：一度誤加 `inputmode="numeric"`，但扁鐵是算式（需打 ＊＋），數字鍵盤打不出來 → 移除，維持全鍵盤。
- 待 Po 確認：若工地實際用 cm 量，要改單位得同步改 upload.html 與 lib/holes.ts 的 parseFlatbar（×10）；預設維持 mm（與現有欄位一致）。

## 追記 3（「累積進度」正名）
- Po 問「累積進度」用意 → 它其實是「各區累積米 ÷ 累積米最多的區」的相對長條，非完成率（沒各區目標當分母，與已拿掉的「整體進度」同毛病）。
- Po 選 A：**改名不改功能** →「累積進度」→「累積量（相對）」＋腳註「滿格＝累積米最多的區，非完成率」。要做真完成率得等 Po 給各區目標數（選項 C）。
