# 2026-07-02 使用體系重整(SSOT / devlog / hooks / skills)

## 做了什麼
- `CLAUDE.md`+`AGENTS.md` 兩份複本 → `CORE_RULES.md` SSOT + 雙 symlink(照 site-assessment 範本)
- 建 `docs/devlog/` + 收工規則寫進 CORE_RULES
- PostToolUse 防呆 hook(`.claude/hooks/postedit-guards.sh`):no-store / timeZone 兩踩坑自動 grep 警告,三情境自測通過
- 新增專案 skill:`/deploy`(部署+線上實測一條龍)、`/ux-audit`(手機截圖巡檢)
- 清掉 10 個殘留 worktree(真實改動存 `~/code/_archive/worktree-patches-2026-07-02/`)+ 移除誤 commit 的 gitlink + gitignore 防再犯

## 為什麼
- AGENTS/CLAUDE 複本已是 drift 未爆彈(Codex 和 Claude 會讀到不同規則且不會被發現)
- 兩個踩坑寫在規則檔三個月仍無執行保證;worktree gitlink 汙染 git status

## 踩了坑
- worktree 只查 `.git` 目錄會漏掉 `.git` 檔(worktree/submodule 形式)→ field-estimation-crane-web 其實是 Engineering Assessment 的 worktree,差點誤 git init
- `.claude/worktrees/` 曾被整批 `git add` 進版控變 gitlink,git status 長期噴雜訊
